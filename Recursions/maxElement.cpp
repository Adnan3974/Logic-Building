#include <iostream>
using namespace std;
int max(int arr[], int size)
{
    if (size < 0)
        return 0;
    int max = 0;
    if max
        > max(arr, size - 1)

                return max[size];
}
int main()
{
    int arr[6]{5, 8, 2, 10, 9, 4};
    max(arr, 5);
    return 0;
}