git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/YourUsername/YourRepo.git

if(origin already exist)
git remote set-url origin https://github.com/YourUsername/NewRepoName.git

# 5. Rename branch to 'main' (only needed if not already named 'main')
git branch -M main

git push -u origin main

# 🔁 Next time, just use:
# git add .
# git commit -m "Your message"
# git push

1. To delete folder from GitHub after deleting locally
git rm -r folder_name/
git commit -m "Something"
git push 
=== Or === 
Simply delete it locally and run the following commands to update the changes
git add -u
git commit -m "Ah! got it"
git push	

